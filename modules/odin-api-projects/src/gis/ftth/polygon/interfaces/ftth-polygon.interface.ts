export interface FtthPolygon {

    id: number,
    geometry?: string,
    l4_closure_id?: number,
    target_release_date?: string,
    // fields we include in gets
    build_status_name?: string,

}
