export const royalMailPafFull = (tblName: string) => [
    `${tblName}.id`,
    `${tblName}.postcode`,
    `${tblName}.posttown`,
    `${tblName}.dependent_locality`,
    `${tblName}.double_dependent_locality`,
    `${tblName}.thoroughfare_and_descriptor`,
    `${tblName}.dependent_thoroughfare_and_descriptor`,
    `${tblName}.building_number`,
    `${tblName}.building_name`,
    `${tblName}.sub_building_name`,
    `${tblName}.po_box`,
    `${tblName}.department_name`,
    `${tblName}.organization_name`,
    `${tblName}.udprn`,
    `${tblName}.postcode_type`,
    `${tblName}.su_organization_indicator`,
    `${tblName}.delivery_point_suffix`,
    `${tblName}.address_key`,
    `${tblName}.organization_key`,
    `${tblName}.number_of_households`,
    `${tblName}.locality_key`,
];
